##!/usr/bin/env zsh
echo $0
echo $PWD
git branch
git branch
exit 1
node -e "let n = 0; const i = setInterval(() => console.log('a' + n++), 1000); setTimeout(() => clearInterval(i), 5100)"
node -e "console.error('test');"
node -e "let n = 0; const i = setInterval(() => console.log('b' + n++), 1000); setTimeout(() => clearInterval(i), 3100)"
node -e "let n = 0; const i = setInterval(() => console.log('c' + n++), 1000); setTimeout(() => clearInterval(i), 7100)"
